"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.monthNames = exports.formatDate = exports.formatTime = exports.dateTo24HourTime = void 0;
function dateTo24HourTime(date) {
    return "".concat(date.getHours().toString().padStart(2, '0'), ":").concat(date.getMinutes().toString().padStart(2, '0'), ":").concat(date
        .getSeconds()
        .toString()
        .padStart(2, '0'), ".").concat(date.getMilliseconds().toString().padStart(3, '0'));
}
exports.dateTo24HourTime = dateTo24HourTime;
function formatTime(timeInMs) {
    if (timeInMs < 1000) {
        return "".concat(timeInMs, " ms");
    }
    else if (timeInMs < 60000) {
        var timeInSec = Math.round(timeInMs / 1000);
        return "".concat(timeInSec, " sec");
    }
    else if (timeInMs < 3600000) {
        var timeInMin = Math.round(timeInMs / 60000);
        return "".concat(timeInMin, " min");
    }
    else {
        var timeInHrs = Math.round(timeInMs / 3600000);
        return "".concat(timeInHrs, " hrs");
    }
}
exports.formatTime = formatTime;
function formatDate(date) {
    var day = date.getDate();
    var suffix = day % 10 === 1 && day !== 11
        ? 'st'
        : day % 10 === 2 && day !== 12
            ? 'nd'
            : day % 10 === 3 && day !== 13
                ? 'rd'
                : 'th';
    return "".concat(day).concat(suffix, " ").concat(exports.monthNames[date.getMonth()], ", ").concat(date.getFullYear());
}
exports.formatDate = formatDate;
exports.monthNames = [
    'January',
    'February',
    'March',
    'April',
    'May',
    'June',
    'July',
    'August',
    'September',
    'October',
    'November',
    'December',
];
