"use strict";
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.spaceTokenList = exports.TokenList = exports.tokenizeStackValue = exports.collapsedTerminatorToken = exports.terminatorToken = exports.namedLambdaToken = exports.lambdaToken = exports.getterValueToken = exports.ellipsesToken = exports.closeBraceToken = exports.openBraceToken = exports.closeBracketToken = exports.openBracketToken = exports.spaceToken = exports.commaToken = exports.lineBreakToken = void 0;
var misc_1 = require("./misc");
exports.lineBreakToken = { type: 'linebreak', value: '\n' };
exports.commaToken = { type: 'punctuation', value: ',' };
exports.spaceToken = { type: 'punctuation', value: ' ' };
exports.openBracketToken = { type: 'punctuation', value: '[' };
exports.closeBracketToken = { type: 'punctuation', value: ']' };
exports.openBraceToken = { type: 'punctuation', value: '{' };
exports.closeBraceToken = { type: 'punctuation', value: '}' };
exports.ellipsesToken = { type: 'punctuation', value: '...' };
exports.getterValueToken = { type: 'punctuation', value: '(...)' };
exports.lambdaToken = { type: 'type', value: 'λ' };
exports.namedLambdaToken = { type: 'type', value: 'λ:' };
exports.terminatorToken = { type: 'terminator', value: '' };
exports.collapsedTerminatorToken = { type: 'terminator', value: '', collapsed: true };
function tokenizeStackValue(data, opts) {
    var _a, _b;
    var stackTokens = TokenList.empty();
    var maxContextLength = Math.min(((_a = data.stack) === null || _a === void 0 ? void 0 : _a.reduce(function (acc, stackEntry) {
        if (stackEntry.context) {
            return stackEntry.context.length > acc ? stackEntry.context.length : acc;
        }
        return acc;
    }, 0)) || 0, 30);
    (_b = data.stack) === null || _b === void 0 ? void 0 : _b.forEach(function (stackEntry, i) {
        var stackEntryContext = stackEntry.context ? (0, misc_1.compactString)(stackEntry.context, maxContextLength) : '';
        stackTokens.add(exports.lineBreakToken);
        if (stackEntryContext) {
            stackTokens.add({
                type: 'function',
                value: stackEntryContext,
                tooltip: stackEntry.context,
            });
        }
        if (stackEntryContext.length < maxContextLength) {
            stackTokens.add({
                type: 'punctuation',
                value: ' '.repeat(maxContextLength - stackEntryContext.length),
            });
        }
        if (maxContextLength > 0) {
            stackTokens.add(exports.spaceToken);
        }
        stackTokens.add({ type: 'punctuation', value: '@ ' });
        stackTokens.add((opts === null || opts === void 0 ? void 0 : opts.linkTokenProvider)
            ? opts.linkTokenProvider(stackEntry)
            : {
                type: 'link',
                value: "".concat((0, misc_1.compactFileName)(stackEntry.source, 50), ":").concat(stackEntry.line, ":").concat(stackEntry.column),
                stackEntry: stackEntry,
            });
    });
    return stackTokens;
}
exports.tokenizeStackValue = tokenizeStackValue;
var TokenList = (function () {
    function TokenList(tokens, length) {
        this.tokens = tokens;
        this.lengthWithoutMultilineFormatting = length;
    }
    TokenList.empty = function () {
        return new TokenList([], 0);
    };
    TokenList.from = function () {
        var tokens = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            tokens[_i] = arguments[_i];
        }
        if (!tokens) {
            return TokenList.empty();
        }
        return new TokenList(tokens, tokens.reduce(function (acc, token) { return acc + singleLineTokenLength(token); }, 0));
    };
    TokenList.prototype.add = function () {
        var results = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            results[_i] = arguments[_i];
        }
        return results.reduce(function (acc, result) {
            var isTokenList = result instanceof TokenList;
            acc.tokens = acc.tokens.concat(isTokenList ? result.tokens : [result]);
            acc.lengthWithoutMultilineFormatting += isTokenList
                ? result.lengthWithoutMultilineFormatting
                : singleLineTokenLength(result);
            return acc;
        }, this);
    };
    TokenList.prototype.removeMultilineFormatting = function () {
        var e_1, _a;
        var tokens = [];
        try {
            for (var _b = __values(this.tokens), _c = _b.next(); !_c.done; _c = _b.next()) {
                var token = _c.value;
                if (token.type === 'indentation') {
                    continue;
                }
                if (token.type === 'linebreak') {
                    tokens.push(exports.spaceToken);
                    continue;
                }
                tokens.push(token);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        this.tokens = tokens;
        return this;
    };
    TokenList.prototype.removeLastToken = function (predicate) {
        var lastToken = this.tokens[this.tokens.length - 1];
        if (lastToken && predicate(lastToken)) {
            var removedToken = this.tokens.pop();
            if (removedToken) {
                this.lengthWithoutMultilineFormatting -= singleLineTokenLength(removedToken);
            }
        }
    };
    return TokenList;
}());
exports.TokenList = TokenList;
exports.spaceTokenList = TokenList.from(exports.spaceToken);
function singleLineTokenLength(token) {
    var _a;
    return token.type === 'indentation' ? 0 : ((_a = token.value) === null || _a === void 0 ? void 0 : _a.length) || 0;
}
