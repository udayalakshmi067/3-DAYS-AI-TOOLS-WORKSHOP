"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.propertyAccessor = exports.getSimplifiedType = exports.getDisplayableType = exports.mergeRequestRangesIntoExpressionsToEvaluate = exports.preparePropsForRangesRequest = void 0;
var keyStrRegExp = /^(?!(?:do|if|in|for|let|new|try|var|case|else|enum|eval|false|null|this|true|void|with|break|catch|class|const|super|throw|while|yield|delete|export|import|public|return|static|switch|typeof|default|extends|finally|package|private|continue|debugger|function|arguments|interface|protected|implements|instanceof)$)[_$a-zA-Z\xA0-\uFFFF][_$a-zA-Z0-9\xA0-\uFFFF]*$/;
var numberRegExp = /^(0|[1-9][0-9]*)$/;
var quotedRegExp = /'([^\\']|\\')*'/;
function preparePropsForRangesRequest(current, props) {
    return ((props === null || props === void 0 ? void 0 : props.reduce(function (acc, p) {
        if (p.rangeNode) {
            var v = __assign(__assign({}, p), { parent: undefined, cappedRange: undefined });
            var rangeIsBeingExpanded = p.id === current.id && p.name === current.name;
            if (p.props) {
                if ((!p.props.length && rangeIsBeingExpanded) || (p.props.length && !p.props[0].rangeNode)) {
                    v.props = true;
                    delete v.capped;
                }
                else {
                    v.props = preparePropsForRangesRequest(current, p.props);
                }
            }
            else if (rangeIsBeingExpanded) {
                v.props = true;
                delete v.capped;
            }
            acc.push(v);
        }
        return acc;
    }, [])) || []);
}
exports.preparePropsForRangesRequest = preparePropsForRangesRequest;
function mergeRequestRangesIntoExpressionsToEvaluate(current, requestRanges) {
    if (!current && !requestRanges) {
        return undefined;
    }
    if (!current) {
        return requestRanges;
    }
    if (!requestRanges) {
        return current;
    }
    var mergedProps = mergeProps(current.props, requestRanges.props);
    return {
        length: Math.max(current.length || 0, requestRanges.length || 0),
        props: mergedProps,
    };
}
exports.mergeRequestRangesIntoExpressionsToEvaluate = mergeRequestRangesIntoExpressionsToEvaluate;
function mergeProps(currentProps, requestProps) {
    var e_1, _a;
    if (!requestProps || !requestProps.length) {
        return currentProps;
    }
    if (!currentProps || !currentProps.length) {
        return requestProps;
    }
    var merged = __spreadArray([], __read(currentProps), false);
    var _loop_1 = function (requestProp) {
        var existingIndex = merged.findIndex(function (currentProp) {
            return currentProp.id === requestProp.id && currentProp.name === requestProp.name;
        });
        if (existingIndex >= 0) {
            var existing = merged[existingIndex];
            if (existing.props === true) {
                return "continue";
            }
            if (requestProp.props === true) {
                merged[existingIndex] = __assign(__assign({}, existing), { props: true });
            }
            else if (existing.props && requestProp.props) {
                merged[existingIndex] = __assign(__assign({}, existing), { props: mergeProps(existing.props, requestProp.props) });
            }
            else if (requestProp.props) {
                merged[existingIndex] = __assign(__assign({}, existing), { props: requestProp.props });
            }
        }
        else {
            return { value: requestProps };
        }
    };
    try {
        for (var requestProps_1 = __values(requestProps), requestProps_1_1 = requestProps_1.next(); !requestProps_1_1.done; requestProps_1_1 = requestProps_1.next()) {
            var requestProp = requestProps_1_1.value;
            var state_1 = _loop_1(requestProp);
            if (typeof state_1 === "object")
                return state_1.value;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (requestProps_1_1 && !requestProps_1_1.done && (_a = requestProps_1.return)) _a.call(requestProps_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return merged;
}
function getDisplayableType(val) {
    if (val.error) {
        return;
    }
    if (val.type === 'array') {
        return val.length && val.length > 5 ? 'Array(' + val.length + ')' : '';
    }
    var type = val.type;
    if (!type) {
        return undefined;
    }
    var normalizedType = type.toLowerCase();
    if (normalizedType === 'function') {
        return 'λ' + (val.funcName ? ' ' + val.funcName : '');
    }
    return normalizedType === 'string' ||
        normalizedType === 'number' ||
        normalizedType === 'symbol' ||
        normalizedType === 'object' ||
        normalizedType === 'null' ||
        normalizedType === 'undefined' ||
        normalizedType === 'boolean' ||
        normalizedType === 'date' ||
        normalizedType === 'regexp'
        ? undefined
        : type;
}
exports.getDisplayableType = getDisplayableType;
function getSimplifiedType(v) {
    return v.type === 'array' ? "Array(".concat(v.length !== undefined ? v.length : '…', ")") : v.type;
}
exports.getSimplifiedType = getSimplifiedType;
function propertyAccessor(name) {
    if (!name)
        return '';
    if (name.match(numberRegExp))
        return '[' + name + ']';
    if (name.match(keyStrRegExp)) {
        return '.' + name;
    }
    return name.match(quotedRegExp) ? '[' + name + ']' : "['" + name + "']";
}
exports.propertyAccessor = propertyAccessor;
